                                                                            
for (let num = 2; num <= 30; num++) { 
    let esPrimo = true; 
    for (let i = 2; i < num; i++) { 
        if (num % i === 0) { 
            // Completar el código...
        } 
    } 
    if (esPrimo) { 
        // Completar el código...
    } 
}